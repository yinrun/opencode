# Patch 02 — HERound close-out

Kernel: `out = rintf(xv)` (round to nearest, ties to even — matches numpy and IEEE 754 default rounding mode).
Result: cosine=1.0, max_abs=0.0, bit-exact, [1,1,128,896], 2 iterations total.

Lesson: any rounding-related op must use `rintf`/`nearbyintf` not `roundf` to match numpy reference.
