# Patch 01 — HEReduceSum close-out

## Approach
Reduce-axis-last，K=896 → output [1,1,128,1]。fp32 累加器 + 单 fp16 round。
Output shape 在 model.cpp 里写死 `{1,1,128,1}`（reduce semantics framework 不自动推断）。

## Result
- cosine = 1.0000000000000002
- max_abs_err = 0.0 (bit-exact)
- shape: input [1,1,128,896] → output [1,1,128,1] fp16
- 1 iteration

## Note
预存于上次 session，本 session 仅做 tensor-compare 验收，0 build / 0 push。
