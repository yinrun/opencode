# Patch 01 — HEReduceMax close-out

## Approach
Reduce 类 op，复用 reduce_sum 模板：单输入 `Op("HEReduceMax", "in0")`，
output shape != input shape (`[1,1,128,128] → [1,1,128,1]`)。
Kernel：fp32 `acc = -INFINITY`，循环求 max，单次 fp16 round。

## Result
- cosine = 0.9999999999999988
- max_abs_err = 0.0 (bit-exact)
- shape: input [1,1,128,128] → output [1,1,128,1] fp16
- 1 iteration
