# Patch 01 — HEAbs close-out

## Approach
Element-wise unary, kernel `out = fabsf(xv)`. 完全复用 silu/rsqrt 模板。

## Pitfall
同 L12 — `build_single_op_model` 模板冲突。手动 link 解决。

## Result
- cosine = 1.0000000000000002
- max_abs_err = 0.0 (bit-exact)
- shape = [1,1,128,896] fp16，114688 元素
- 2 iterations
