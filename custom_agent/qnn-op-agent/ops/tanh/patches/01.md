# Patch 01 — HETanh close-out

## Approach
Element-wise unary，kernel `out = tanhf(xv)`。L10 预言：tanh 是 silu 模板的零修改克隆。

## Result
- cosine = 1.0
- max_abs_err = 0.0 (bit-exact)
- shape = [1,1,128,896] fp16，114688 元素
- 1 iteration

## Verification
直接复用 silu/exp 模板 + kernel 一行替换，0 patches，验收一次过。
