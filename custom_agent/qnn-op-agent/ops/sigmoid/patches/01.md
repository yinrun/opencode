# Sigmoid Iter 1 — replay artifacts

## Hypothesis
进入 session 时发现 ops/sigmoid/build/ 已有 src/HESigmoid.cpp + model_hesigmoid.cpp +
hexagon-v81/aarch64-android 编译产物 + model libQnnHESigmoidModel.so + device_configs/*.json，
且 ops/sigmoid/outputs/device/Result_0/output_native.raw 时间戳 14:19、reference 时间戳 14:18，
设备运行已发生过、只是 state.json 没翻成 completed（与 L6 div 同模式）。

## Action
直接对已有产物跑 tensor-compare_compare_pass，验证是否真的达标。

## Verification
- cosine_similarity = 0.9999999999998367
- max_abs_error = 0.000244140625 (= 2 fp16 ULP at magnitude 1)
- relative_error = 7.86e-10
- shapes_match = True, shape = [622592] = 1*1*128*4864
- passed = true（cosine ≥ 0.999 且 max_abs ≤ 1e-3）

## Result
Pass。零额外 build/run，1 iteration（即 L6 操作教训：复活 in_progress op 先查产物再决定动作）。

## Artifacts
- ops/sigmoid/build/src/HESigmoid.cpp（kernel: `1.0f / (1.0f + expf(-xv))`）
- ops/sigmoid/build/src/HESigmoidInterface.cpp
- ops/sigmoid/build/model_src/model_hesigmoid.cpp（getGraphInfoFromModels, validations=0）
- ops/sigmoid/build/build/hexagon-v81/libQnnHtpSigmoid.so
- ops/sigmoid/build/build/aarch64-android/libQnnHtpSigmoid.so
- ops/sigmoid/build/model/libQnnHESigmoidModel.so
- ops/sigmoid/build/device_configs/{htp_config,net_run_config}.json
- ops/sigmoid/inputs/in.raw + input_list.txt
- ops/sigmoid/outputs/reference/output_ref.raw
- ops/sigmoid/outputs/device/Result_0/output_native.raw
