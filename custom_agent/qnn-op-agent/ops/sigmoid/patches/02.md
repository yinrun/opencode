# Patch 02 — HESigmoid close-out (re-validation)

本次 session 只做 tensor-compare 验收，无新 build / push。

## Result
- cosine = 0.9999999999998367
- max_abs_err = 2.44e-4 (≈ 2 fp16 ULP at magnitude 1)
- shape = [1,1,128,4864] fp16
- 1 iteration

复用 L10/L11 结论：sigmoid 路径 `1/(1+exp(-x))` 比 numpy 等价计算多 2 步 fp32 op，2 ULP 累积。
