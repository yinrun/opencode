# Patch 01 — HENeg close-out

## Approach
Element-wise unary, kernel `out = -xv`. 完全复用 silu/rsqrt 模板。

## Pitfall
同 L12 (sqrt patches/01.md) — `build_single_op_model` overwrite 导致首轮 finalize 错误。
Fix: 手动 link 用 `getGraphInfoFromModels()`。

## Result
- cosine = 1.0000000000000002
- max_abs_err = 0.0 (bit-exact)
- shape = [1,1,128,896] fp16，114688 元素
- 2 iterations
