# Patch 01 — HEExp recovery & close-out

## Hypothesis
上次 session 已经把 HEExp 走完 build → push → run → pull 全流程，但是
进程崩溃前没更新 `state.json`（exp 字段还是 `in_progress / iterations=0`）。
按照 L6 经验，先复活后检查产物，不直接重跑。

## Verification
1. 检查 `ops/exp/outputs/device/output_native.raw`（32768 bytes，5月29 15:40）和
   `ops/exp/outputs/ref/out_ref.raw`（5月29 15:34）都存在、时间戳同一 session。
2. `tensor-compare_compare_pass`（cosine_min=0.999, max_abs_max=1e-3, fp16）。

## Result
- cosine_similarity = 1.0
- max_abs_error = 0.0
- relative_error = 0.0
- shape = [1,1,128,128]，16384 fp16 元素
- bit-exact：device 与 ref 的 uint16 view `array_equal` 为 True
- input 范围 [-4.39, 4.15]，远低于 fp16 exp 溢出阈值 11.09，没踩 overflow 陷阱
- ref 范围 [0.0124, 63.59]，全程有限正数

## Implementation note
Kernel 跟 silu/div 同构：
```cpp
out(n,h,w,d) = expf(in(n,h,w,d));   // fp16 → fp32 promote、expf、单次 fp16 round
```
注册 3 个 variant + `Flat("*"), MainMemory("*")`。

## Decision
Mark `exp` 为 `completed`，1 iteration（recovery，无新 patch 需要）。

## Lesson
L6 已经 cover 这个场景。本 op 没有新 lesson 要追加。
